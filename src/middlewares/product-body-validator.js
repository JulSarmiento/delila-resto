/**
 * Product body validator middleware
 *
 * @param {e.Request} request - The Request
 * @param {e.Response} response - The Response
 * @param {Function} next - Trigger next route
 */
module.exports = (request, response, next) => {
  next()
}
